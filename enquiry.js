const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));(async()=>{const q=new URLSearchParams(location.search),ids=(q.get('ids')||'').split(',').map(x=>x.trim()).filter(Boolean),type=q.get('type')==='wholesale'?'Wholesale':'Retail',root=document.getElementById('enquiryProducts');document.getElementById('enquiryType').textContent=`${type} enquiry · ${ids.length} selected model${ids.length===1?'':'s'}`;if(!ids.length){root.innerHTML='<p class="empty">No selected dhoti models were found in this enquiry link.</p>';return}try{const r=await fetch('/api/products',{cache:'no-store'}),d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to load catalogue');const byId=new Map((d.products||[]).map(p=>[String(p.id),p])),selected=ids.map(id=>byId.get(String(id))).filter(Boolean);if(!selected.length){root.innerHTML='<p class="empty">These products are no longer available in the catalogue.</p>';return}root.innerHTML=selected.map((p,i)=>`<article class="enquiry-card"><div class="photo"><img src="${esc(p.image_1||'')}" alt="${esc(p.product_code||`Dhoti ${i+1}`)}" loading="lazy"></div><div class="info"><span class="number">${String(i+1).padStart(2,'0')}</span><h2>${esc(p.product_code||`Dhoti ${i+1}`)}</h2><p>${esc(p.category||'Nivetha Dhoti')}</p></div></article>`).join('')}catch(e){root.innerHTML=`<p class="empty">We couldn't load the selected dhoti images.<br>${esc(e.message)}</p>`}})();